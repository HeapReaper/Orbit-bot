import { Application } from "express";
import { Client } from "discord.js";

export default function registerApi(app: Application, client: Client) {
  // app.get("/api/AutoRole", (req, res) => {
  //   res.json({ message: "Hello from AutoRole!" });
  // });
}
